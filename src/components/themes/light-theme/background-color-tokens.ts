import { colorTokens } from '../shared/color-tokens'
import { BackgroundColorTokens } from '../types'

export const backgroundColorTokens: BackgroundColorTokens = {
  backgroundPrimary: colorTokens.white,
  backgroundSecondary: colorTokens.gray50,
  backgroundAccent: colorTokens.blue500,
  backgroundSuccess: colorTokens.green500,
  backgroundWarning: colorTokens.orange500,
  backgroundDanger: colorTokens.red500,
  backgroundAccentLight: colorTokens.blue50,
  backgroundSuccessLight: colorTokens.green50,
  backgroundWarningLight: colorTokens.orange50,
  backgroundDangerLight: colorTokens.red50,
}
